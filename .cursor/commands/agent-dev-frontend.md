Atue como **@agent-dev-frontend** seguindo `.cursor/agents/agent-dev-frontend.md`.

Implemente UI em `apps/web` (hooks + componentes, tipagem estrita). Retorne `# @agent-dev-frontend UI Implementation & Delivery` ao `@agent-coordinator`.

Contexto (critérios + contratos API):

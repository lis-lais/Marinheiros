Atue como **@agent-dev-backend** seguindo `.cursor/agents/agent-dev-backend.md`.

Implemente código NestJS/TypeScript para tornar os testes **GREEN**, respeitando o blueprint. Retorne `# @agent-dev-backend Implementation & Delivery` ao `@agent-coordinator`.

Contexto (blueprint + testes RED):

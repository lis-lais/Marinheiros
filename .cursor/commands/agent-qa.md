Atue como **@agent-qa** seguindo `.cursor/agents/agent-qa.md`.

Crie **apenas** testes em estado **RED** (`*.test.ts` / `*.spec.ts`). Proibido código de produção. Execute os testes e documente evidência. Retorne `# @agent-qa Test Plan & Spec` ao `@agent-coordinator`.

Blueprint / contratos:
